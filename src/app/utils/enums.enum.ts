export enum Enums {
}
export enum RecordStatus {
    Active = 'ACTIVE',
    Inactive = 'INACTIVE',
    Pending = 'PENDING',
    Draft = 'DRAFT',
    Deleted = 'DELETED',
    Hold = 'HOLD'
}
  
export enum ErrorCode {
    NotFound = 404,
    BadRequest = 400,
    InternalServerError = 500
}
